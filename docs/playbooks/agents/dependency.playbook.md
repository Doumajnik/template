+++
id = "agents/dependency"
title = "Dependency Agent Rules"
agents = ["dependency"]
technologies = ["all"]
category = "rule"
tags = ["dependency"]
version = 1
+++

### Dependency Guidelines

Agent-specific rules and patterns for the dependency agent. Add rules here as they are discovered during development.
