+++
id = "agents/type-safety"
title = "Type Safety Agent Rules"
agents = ["type-safety"]
technologies = ["all"]
category = "rule"
tags = ["type-safety"]
version = 1
+++

### Type Safety Guidelines

Agent-specific rules and patterns for the type-safety agent. Add rules here as they are discovered during development.
