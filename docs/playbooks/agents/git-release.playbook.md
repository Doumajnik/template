+++
id = "agents/git-release"
title = "Git Release Agent Rules"
agents = ["git-release"]
technologies = ["all"]
category = "rule"
tags = ["git-release"]
version = 1
+++

### Git Release Guidelines

Agent-specific rules and patterns for the git-release agent. Add rules here as they are discovered during development.
