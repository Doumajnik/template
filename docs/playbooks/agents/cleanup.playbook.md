+++
id = "agents/cleanup"
title = "Cleanup Agent Rules"
agents = ["cleanup"]
technologies = ["all"]
category = "rule"
tags = ["cleanup"]
version = 1
+++

### Cleanup Guidelines

Agent-specific rules and patterns for the cleanup agent. Add rules here as they are discovered during development.
