+++
id = "agents/migration"
title = "Migration Agent Rules"
agents = ["migration"]
technologies = ["all"]
category = "rule"
tags = ["migration"]
version = 1
+++

### Migration Guidelines

Agent-specific rules and patterns for the migration agent. Add rules here as they are discovered during development.
