+++
id = "agents/test-writer"
title = "Test Writer Agent Rules"
agents = ["test-writer"]
technologies = ["all"]
category = "rule"
tags = ["test-writer"]
version = 1
+++

### Test Writer Guidelines

Agent-specific rules and patterns for the test-writer agent. Add rules here as they are discovered during development.
