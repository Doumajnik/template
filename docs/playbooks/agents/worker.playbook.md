+++
id = "agents/worker"
title = "Worker Agent Rules"
agents = ["worker"]
technologies = ["all"]
category = "rule"
tags = ["worker"]
version = 1
+++

### Worker Guidelines

Agent-specific rules and patterns for the worker agent. Add rules here as they are discovered during development.
