+++
id = "agents/debug"
title = "Debug Agent Rules"
agents = ["debug"]
technologies = ["all"]
category = "rule"
tags = ["debug"]
version = 1
+++

### Debug Guidelines

Agent-specific rules and patterns for the debug agent. Add rules here as they are discovered during development.
