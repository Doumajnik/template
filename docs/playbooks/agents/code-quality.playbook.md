+++
id = "agents/code-quality"
title = "Code Quality Agent Rules"
agents = ["code-quality"]
technologies = ["all"]
category = "rule"
tags = ["code-quality"]
version = 1
+++

### Code Quality Guidelines

Agent-specific rules and patterns for the code-quality agent. Add rules here as they are discovered during development.
