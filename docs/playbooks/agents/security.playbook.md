+++
id = "agents/security"
title = "Security Agent Rules"
agents = ["security"]
technologies = ["all"]
category = "rule"
tags = ["security"]
version = 1
+++

### Security Guidelines

Agent-specific rules and patterns for the security agent. Add rules here as they are discovered during development.
