+++
id = "agents/reviewer"
title = "Reviewer Agent Rules"
agents = ["reviewer"]
technologies = ["all"]
category = "rule"
tags = ["reviewer"]
version = 1
+++

### Reviewer Guidelines

Agent-specific rules and patterns for the reviewer agent. Add rules here as they are discovered during development.
