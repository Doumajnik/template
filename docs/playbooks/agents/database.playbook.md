+++
id = "agents/database"
title = "Database Agent Rules"
agents = ["database"]
technologies = ["all"]
category = "rule"
tags = ["database"]
version = 1
+++

### Database Guidelines

Agent-specific rules and patterns for the database agent. Add rules here as they are discovered during development.
