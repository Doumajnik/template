+++
id = "shared/testing-rules"
title = "Testing Rules"
agents = ["all"]
technologies = ["all"]
category = "rule"
tags = ["testing", "tdd", "coverage"]
version = 1
+++

### Testing Rules

Write 15+ tests per function covering: happy paths, edge cases, error scenarios, boundary conditions, and integration. Tests are written BEFORE implementation (TDD red phase). Test files mirror `src/` structure in `tests/`.
