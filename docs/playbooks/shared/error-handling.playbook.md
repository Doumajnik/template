+++
id = "shared/error-handling"
title = "Error Handling Rule"
agents = ["all"]
technologies = ["all"]
category = "rule"
tags = ["error-handling", "exceptions", "logging"]
version = 1
+++

### Error Handling

Never swallow errors silently. Every catch block must log, re-throw, or return a meaningful error. Fix errors/warnings proactively — zero errors, zero warnings always.
