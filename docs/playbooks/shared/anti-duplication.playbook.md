+++
id = "shared/anti-duplication"
title = "Anti-Duplication Rule"
agents = ["all"]
technologies = ["all"]
category = "rule"
tags = ["duplication", "dedup", "code-reuse"]
version = 1
+++

### Anti-Duplication

Before creating any new function, class, or constant, search `CODE_INVENTORY.md` for similar existing symbols. If a match exists, reuse or extend it instead of creating a duplicate.
