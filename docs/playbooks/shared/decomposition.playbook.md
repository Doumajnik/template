+++
id = "shared/decomposition"
title = "Decomposition Rule"
agents = ["all"]
technologies = ["all"]
category = "rule"
tags = ["decomposition", "function-size", "single-responsibility"]
version = 1
+++

### Decomposition

Functions must be ≤40 lines. If a function does more than one thing (needs 'and' to describe it), split it. Single Responsibility Principle applies to every function.
