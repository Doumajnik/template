+++
id = "shared/markdown-formatting"
title = "Markdown Formatting Convention"
agents = ["all"]
technologies = ["all"]
category = "convention"
tags = ["markdown", "formatting", "documentation"]
version = 1
+++

### Markdown Formatting

Always use blank lines around lists, fenced code blocks, and headings. Tables must have header separators. Use proper heading hierarchy (no skipping levels).
